const moduleSelect=document.querySelector('#module'),quiz=document.querySelector('#quiz'),state=document.querySelector('#state'),result=document.querySelector('#result');
let questions=[];
const moduleId=()=>Number(moduleSelect.value);
async function load(){result.innerHTML='';quiz.innerHTML='';state.textContent='Carregando avaliação...';const response=await fetch(`/api/assessments/${moduleId()}`);if(!response.ok){state.textContent='Entre na sua conta para realizar e salvar esta avaliação.';return}const data=await response.json();questions=data.questions;state.textContent=`${data.title} · ${data.attempts.length ? `melhor tentativa recente: ${data.attempts[0].score}%` : 'nenhuma tentativa ainda'}`;quiz.innerHTML=questions.map((question,index)=>`<fieldset class="question"><h2>${String(index+1).padStart(2,'0')} · ${question.prompt}</h2>${question.options.map((option,optionIndex)=>`<label class="option"><input required type="radio" name="q${index}" value="${optionIndex}">${option}</label>`).join('')}</fieldset>`).join('')}
moduleSelect.value=new URLSearchParams(location.search).get('module')||'1';moduleSelect.onchange=load;
quiz.onsubmit=async event=>{event.preventDefault();const answers=questions.map((_,index)=>Number(new FormData(quiz).get(`q${index}`)));const response=await fetch(`/api/assessments/${moduleId()}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answers})});if(!response.ok){state.textContent='Não foi possível salvar. Entre novamente e tente.';return}const data=await response.json();result.className=`result ${data.passed?'':'fail'}`;result.innerHTML=`<h2>${data.score}% · ${data.passed?'Aprovado!':'Ainda não aprovado'}</h2><p>${data.passed?'Módulo aprovado. Badge liberada no perfil.':'Você precisa de 70%. Revise as explicações e tente novamente.'}</p>${data.answers.map((answer,index)=>`<div class="feedback"><b>Questão ${index+1}: ${answer.selected===answer.correct?'correta':'reveja esta resposta'}</b><br>${answer.explanation}</div>`).join('')}`;window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'})};
void load();
